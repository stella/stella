{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [
    ".oxfmtrc.json",
    "Cargo.toml",
    "Cargo.lock",
    "rust-toolchain.toml",
    "fixtures/docx-runtime-parity.json",
    "crates/**"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": [
        "STELLA_ANONYMIZE_NATIVE_PACKAGE_LANGUAGES",
        "STELLA_ANONYMIZE_SKIP_WASM_BUILD",
        "STELLA_ANONYMIZE_WASM_PACKAGE_LANGUAGES"
      ],
      "outputs": [
        "dist/**",
        "wasm/dist/**",
        "native-wasm-dist/**",
        "default-pipeline-input.json.gz",
        "native-pipeline.stlanonpkg",
        "native-pipeline.*.stlanonpkg"
      ]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {},
    "format": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
