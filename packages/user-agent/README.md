# @stll/user-agent

Browser and OS names parsed from a user-agent string for session and device lists.

## What lives here

Browser and OS names parsed from a user-agent string for session and device lists, and the tests that pin its behaviour.

## What does not

Anything outside that concern: app-specific wiring, one-off helpers, and
code another package already owns.

## License

Apache-2.0
