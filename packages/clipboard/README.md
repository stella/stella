# @stll/clipboard

Result-wrapped clipboard writes for browser code.

## What lives here

Result-wrapped clipboard writes for browser code, and the tests that pin its behaviour.

## What does not

Anything outside that concern: app-specific wiring, one-off helpers, and
code another package already owns.

## License

Apache-2.0
