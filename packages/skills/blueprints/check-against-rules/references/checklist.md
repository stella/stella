# Checklist

<!-- guide: One row per thing to check. Be specific about the threshold that turns something into a finding. The workflow in SKILL.md reads this file, so keep it scannable. -->

| Code | Check                       | Flag when…                 |
| ---- | --------------------------- | -------------------------- |
| C1   | > e.g. Confidentiality term | > e.g. longer than 5 years |
| C2   | > e.g. Governing law        | > e.g. missing             |

> Delete the example rows and write your own.
