diff --git a/dist/esm/activities/chat/stream/processor.js b/dist/esm/activities/chat/stream/processor.js
index 1339fa028d5cad2b586bc9c838b5b9f97f74204e..ac6ac06b1181ac7aae9d1e3e6fe6e9b342445fbe 100644
--- a/dist/esm/activities/chat/stream/processor.js
+++ b/dist/esm/activities/chat/stream/processor.js
@@ -37,6 +37,15 @@ function interruptBatchHasGeneric(interrupts) {
 		return binding !== null && typeof binding === "object" && !Array.isArray(binding) && binding.kind === "generic";
 	});
 }
+function serializeToolInput(input) {
+	if (input === void 0) return void 0;
+	try {
+		const serialized = JSON.stringify(input);
+		return typeof serialized === "string" ? serialized : void 0;
+	} catch {
+		return void 0;
+	}
+}
 /**
 * StreamProcessor - State machine for processing AI response streams
 *
@@ -837,19 +846,22 @@ var StreamProcessor = class StreamProcessor {
 		if (!msgState) return;
 		const input = chunk.input !== void 0 ? chunk.input : tanstackMetadata(chunk)?.input;
 		const existingToolCall = msgState.toolCalls.get(chunk.toolCallId);
-		if (existingToolCall && existingToolCall.state !== "input-complete") {
-			if (input !== void 0 && !existingToolCall.arguments) try {
-				existingToolCall.arguments = JSON.stringify(input);
-			} catch {}
-			const index = msgState.toolCallOrder.indexOf(chunk.toolCallId);
-			this.completeToolCall(messageId, index, existingToolCall);
-			if (input !== void 0) {
+		if (existingToolCall) {
+			const serializedInput = serializeToolInput(input);
+			if (serializedInput !== void 0) existingToolCall.arguments = serializedInput;
+			const wasComplete = existingToolCall.state === "input-complete";
+			if (!wasComplete) {
+				const index = msgState.toolCallOrder.indexOf(chunk.toolCallId);
+				this.completeToolCall(messageId, index, existingToolCall);
+			}
+			if (serializedInput !== void 0) {
 				existingToolCall.parsedArguments = input;
+				const currentPart = wasComplete ? this.messages.find((m) => m.id === messageId)?.parts.find((p) => p.type === "tool-call" && p.id === existingToolCall.id) : void 0;
 				this.messages = updateToolCallPart(this.messages, messageId, {
 					id: existingToolCall.id,
 					name: existingToolCall.name,
 					arguments: existingToolCall.arguments,
-					state: "input-complete",
+					state: currentPart?.state ?? "input-complete",
 					input,
 					...existingToolCall.metadata !== void 0 && { metadata: existingToolCall.metadata }
 				});
diff --git a/dist/esm/activities/chat/tools/tool-calls.js b/dist/esm/activities/chat/tools/tool-calls.js
index 240c7373c037311973b4e304e59071a6b7660a56..d982a23b7ca8cf8c009f9db383c3f40e36d39b38 100644
--- a/dist/esm/activities/chat/tools/tool-calls.js
+++ b/dist/esm/activities/chat/tools/tool-calls.js
@@ -420,9 +420,12 @@ async function* executeToolCalls(toolCalls, tools, approvals = /* @__PURE__ */ n
 			continue;
 		}
 		if (hasPendingApprovals) {
+			// Only an already approved call waits for the rest of its step's
+			// approvals. A call that needs no approval runs now, like a client
+			// request, so no server call ends a run without its result.
 			const isPendingApproval = tool.needsApproval && approvalResolution(approvals, toolCall.id) === void 0;
-			const isPlainClientRequest = !tool.needsApproval && !tool.execute;
-			if (!isPendingApproval && !isPlainClientRequest) continue;
+			const isNoApprovalCall = !tool.needsApproval;
+			if (!isPendingApproval && !isNoApprovalCall) continue;
 		}
 		if (resumeState?.cancelledToolCallIds?.has(toolCall.id)) {
 			results.push({
