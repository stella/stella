---
"@stll/ui": patch
---

Add the `chrome-raised` overlay layer, one step above `chrome`, for in-pane controls that float over the docked composer. The composer's veil blurs what lies beneath it, so a control in the same band stayed sharp only when it happened to mount after the composer.
