---
"@stll/ui": minor
---

`FileInput` picks a single file behind a translated `Button` and shows the chosen name, replacing the browser's untranslatable "Choose file" chrome. `Input` no longer accepts `type="file"`.
