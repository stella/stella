---
"@stll/cli": minor
---

Mark commands the connected server has gated off everywhere they are listed: root `--help` and group briefs name disabled groups and children, and capability commands are marked from the server's new `x-stella-feature-omitted-capabilities` evidence in `--help` and `tools list`.
