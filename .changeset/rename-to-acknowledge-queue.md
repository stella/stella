---
"@stll/cli": patch
---

Rename the `work-obligations.queues.list` queue value `inbox` to `to_acknowledge`, so the governed My Work queue no longer collides with the inbox signal feed. The capability description names the new queue.
