---
"@stll/cli": minor
---

`stella capability entity-views rows-list` accepts `inboxView` in its input to return the caller's Inbox signals for that view in the same window as tasks, ordered by the same sorts under one cursor, and `--as-of` for the calendar day task risk is measured against.
