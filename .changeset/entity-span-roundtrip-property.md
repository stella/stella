---
---

No release: pin the `generated_entity_spans_fail_or_round_trip` counterexample and correct the property's entity generator, which gave every entity the same display text and therefore the same placeholder identity.
