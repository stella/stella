---
"@stll/cli": minor
---

Refresh the capability catalog with the inbox signal capabilities (list, read, count, request, snooze, dismiss, assign, accept), retain the governed `my-work` queue for the Inbox work view, and drop the retired `my-tasks` capability.
