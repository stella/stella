---
"@stll/cli": minor
---

Refresh the capability catalog with the inbox signal capabilities (list, read, count, request, snooze, dismiss, assign, accept) and drop the retired `my-tasks` and `my-work` queue capabilities.
