---
"@stll/ui": patch
"@stll/workspace-ui": patch
---

Export the inspector layout tokens as `@stll/ui/inspector/layout-tokens`, and export the shared field-value parts (`formatIntFieldValue`, `PersonAvatar`, `PendingSkeleton`, the empty, error, unsupported and clip renderers, and the select and int styling helpers) from `@stll/workspace-ui/field-value`.
