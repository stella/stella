---
"@stll/api-contract": minor
"@stll/cli": patch
---

Expose `LIST_ITEM_TYPE` as the named source for list-row discriminators and refresh the `work-obligations.queues.list` capability description to state that the queues partition the owner's work.
