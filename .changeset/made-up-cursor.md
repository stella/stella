---
"@stll/cli": patch
---

Regenerate the MCP registry snapshot: `search_boe_legislation` no longer declares a five-character ceiling on `cursor`; the server reads a value no page boundary could be as no cursor.
