---
"@stll/cli": minor
---

Adds `stella capability time-entries suggestions-list` and `stella capability time-entries suggestions-decisions-create`: list suggested time entries for a day from the signed-in user's own matter activity, then accept one into a time entry or dismiss it.
