---
"@stll/ui": patch
---

The sidebar exports `useSidebarLayout()` (`"rail"` or `"full"`) and `sidebarIdentityTriggerClassName(layout)`, one geometry for an avatar trigger in the footer: centred in the collapsed desktop rail, full size in the expanded sidebar and the mobile sheet.
