---
"@stll/workspace-ui": minor
"@stll/cli": patch
---

Add an optional `maxChildren` cap to the condition builder's capabilities; refresh the capability catalog with the bounded request filter arrays.
