---
"@stll/ui": minor
---

`Button` and `CommandItem` gain a `row` size built from one shared metric, and `MenuSection` owns the inset, heading and row stack of a titled group of menu rows, so sibling lists in one panel share a density.
