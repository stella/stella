---
"@stll/cli": patch
---

`template save`'s `--docx-base64` help now states that the flag carries the original file's bytes encoded verbatim, and that parts must never be stripped out to shrink it.
