---
"@stll/conditions": minor
"@stll/ui": minor
"@stll/workspace-ui": minor
---

Add agenda-kind filters and shared view-toolbar and Kanban proposal primitives. Replace view-switcher `renderActions` with `actionMenu`, which owns the vertical menu trigger. Hide redundant empty-lane counts and let empty Kanban cells scroll with the board.
