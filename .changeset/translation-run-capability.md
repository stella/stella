---
"@stll/cli": minor
---

Translation goes through the background run: `stella capability document-translations runs-create` starts one and `runs-get` reads its progress and output. The synchronous `stella capability entities translate` command is gone.
