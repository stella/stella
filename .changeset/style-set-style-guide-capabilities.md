---
"@stll/cli": patch
---

Regenerate the capability catalog: a style set now exposes the paragraph styles it uses (`style-sets.style-catalogue.get`) and takes a written style guide for them (`style-sets.style-guide.update`), which is what converting a document into that style set reads.
