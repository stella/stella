---
"@stll/ui": minor
---

Tooltips render on the transient popup layer, so a tooltip opened from a dialog covers it. `TooltipPopup` no longer accepts `layer` and `Button` no longer accepts `tooltipLayer`.
