name: Provenance Nightly

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  provenance-update:
    permissions:
      contents: write
      pull-requests: write
    uses: stella/.github/.github/workflows/provenance-update.yml@68f07b8d089564fcbd9383824ddc8af31ff8d4ba
    secrets:
      app_id: ${{ secrets.PROVENANCE_APP_ID }}
      app_private_key: ${{ secrets.PROVENANCE_APP_PRIVATE_KEY }}
    with:
      install-command: bun install --frozen-lockfile --ignore-scripts
      provenance-version: v0.1.3
