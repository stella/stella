name: Package quarantine policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce package quarantine
    if: github.repository == 'stella/anonymize'
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/quarantine-policy.yml@699affd63b2d956f71b9833fbed239245aff7e86
