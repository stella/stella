---
// Starlight override: the landing's two-state toggle replaces Starlight's
// three-option select so both halves of the site share one control.
import ThemeToggle from "../ThemeToggle.astro";
---

<ThemeToggle />
