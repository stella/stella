---
import { defaultLocale, isLocale } from "../i18n/config";
import { getTranslations } from "../i18n/utils";

const rawLocale = Astro.currentLocale ?? defaultLocale;
const locale = isLocale(rawLocale) ? rawLocale : defaultLocale;
const t = getTranslations(locale);
---

<section class="px-6 py-10 lg:py-14" aria-labelledby="mcp-proof-title">
  <div class="mcp-proof mx-auto max-w-5xl border-x">
    <div class="px-6 py-16 sm:px-12 sm:py-20 lg:px-20">
      <h2
        id="mcp-proof-title"
        class="max-w-4xl font-display text-3xl font-medium leading-[1.14] tracking-tight sm:text-4xl lg:text-5xl"
      >
        {t("mcpProof.title")}
        <span style="color: var(--muted-foreground)">
          {t("mcpProof.subtitle")}
        </span>
      </h2>
    </div>

    <div class="grid border-t md:grid-cols-3">
      <article class="mcp-proof-card p-6 sm:p-8">
        <div class="flex items-center gap-3">
          <svg aria-hidden="true" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.7">
            <path d="M7.5 4.5h9a3 3 0 0 1 3 3v9a3 3 0 0 1-3 3h-9a3 3 0 0 1-3-3v-9a3 3 0 0 1 3-3Z" />
            <path d="M9 9h6M9 12h4M9 15h2" />
          </svg>
          <h3 class="font-display text-xl font-medium">{t("mcpProof.contextTitle")}</h3>
        </div>
        <p class="mt-3 text-base leading-relaxed" style="color: var(--muted-foreground)">
          {t("mcpProof.contextBody")}
        </p>
      </article>

      <article class="mcp-proof-card p-6 sm:p-8">
        <div class="flex items-center gap-3">
          <svg aria-hidden="true" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.7">
            <path d="m12 3 1.5 5.5L19 10l-5.5 1.5L12 17l-1.5-5.5L5 10l5.5-1.5L12 3Z" />
            <path d="m18.5 15 .7 2.3 2.3.7-2.3.7-.7 2.3-.7-2.3-2.3-.7 2.3-.7.7-2.3Z" />
          </svg>
          <h3 class="font-display text-xl font-medium">{t("mcpProof.toolsTitle")}</h3>
        </div>
        <p class="mt-3 text-base leading-relaxed" style="color: var(--muted-foreground)">
          {t("mcpProof.toolsBody")}
        </p>
      </article>

      <article class="mcp-proof-card p-6 sm:p-8">
        <div class="flex items-center gap-3">
          <svg aria-hidden="true" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.7">
            <path d="M12 3.5 19 7v5c0 4.2-2.6 7.1-7 8.5-4.4-1.4-7-4.3-7-8.5V7l7-3.5Z" />
            <path d="M9.5 12.2 11.2 14l3.6-4" />
          </svg>
          <h3 class="font-display text-xl font-medium">{t("mcpProof.scopeTitle")}</h3>
        </div>
        <p class="mt-3 text-base leading-relaxed" style="color: var(--muted-foreground)">
          {t("mcpProof.scopeBody")}
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .mcp-proof,
  .mcp-proof-card {
    border-color: var(--border);
  }

  .mcp-proof {
    background-image: repeating-linear-gradient(
      135deg,
      transparent 0 7px,
      color-mix(in srgb, var(--foreground) 4%, transparent) 7px 8px
    );
  }

  .mcp-proof > * {
    background: var(--background);
  }

  .mcp-proof-card + .mcp-proof-card {
    border-top: 1px solid var(--border);
  }

  @media (min-width: 48rem) {
    .mcp-proof-card + .mcp-proof-card {
      border-top: 0;
      border-inline-start: 1px solid var(--border);
    }
  }
</style>
