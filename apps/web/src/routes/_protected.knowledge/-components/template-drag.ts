/** Custom MIME carrying a dragged template's id between a template row and the
 *  category drop targets. A custom type keeps real file drops (DOCX upload)
 *  distinct from template-reassignment drags. */
export const TEMPLATE_DRAG_MIME = "application/x-stella-template-id";
