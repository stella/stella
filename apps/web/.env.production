# Production-only public empty-state media. Vite does not load this file
# in local dev, so local instances render the bundled poster fallback.
VITE_PUBLIC_APP_URL="https://stll.app"
VITE_EMPTY_STATE_MATTERS_VIDEO_URL="https://assets.stll.app/empty-states/matters-intro.mp4"
