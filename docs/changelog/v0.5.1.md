# Maintenance release

## Reliability fixes for large case-law corpora.

Case-law decisions get their public URL slug when they are ingested. This
release makes the slug uniqueness index scale to very large corpora by
building it without locking writes, and moves the one-time backfill of older
decisions into a resumable batch job. No user-facing changes.
