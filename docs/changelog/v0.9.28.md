# Maintenance release

Stella includes reliability and maintenance improvements.

## Packages

- @stll/auth-model: Pin the contract to Better Auth 1.7.4 and drop `account.issuer`, which the library no longer declares; the retained column is now a host field.
